# RoomBook

会議室予約の小さな React + Vite アプリ。バックエンドなし、state はブラウザ内のみ。

## ルール

- UI の文言は日本語。
- 新しいライブラリは追加しない（React / Vite のみ）。
- ページは `src/pages/` に 1 ファイル 1 画面。共通データは `src/data.js`。
- スタイルは `src/index.css` に集約。CSS 変数（`--accent` など）を使う。
- 変更後は `npm run build` が通ることを確認する。
